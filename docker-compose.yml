version: '1'

services:

  db:
    image: postgres:16.0
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: zli_ludzie_na_omatko
      POSTGRES_DB: licznik_db

    ports:
      - "5432:5432"
    volumes:
      - ${PWD}/postgress_data:/var/lib/postgresql/data
